# Setup (EXAMPLE)

This entry is not published. There is no share link (`import` is empty).

If this were a real template:

1. Open the official `https://x.ai/bot/…` link and choose Add to Grok Bot.
2. Reconnect Gmail yourself if you want mail as a source. Do not paste API keys here.
3. After import, inspect skills. Preview can show skills while the export arrives empty.
4. Run the first safe task read-only: summarize this conversation in three bullets. Do not send mail or create events.
5. Do not enable routines until that task looks right.
